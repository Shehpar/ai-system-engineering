apiVersion: v1
kind: Secret
metadata:
  name: anomaly-detector-secrets
  labels:
    app: anomaly-detector
type: Opaque
stringData:
  # Replace these with actual base64 encoded values in production
  influxdb_password: "admin_password_change_me"
  grafana_admin_password: "admin_change_me"
  mlflow_db_password: "mlflow_password_change_me"
  # For production, create secrets using:
  # kubectl create secret generic anomaly-detector-secrets \
  #   --from-literal=influxdb_password='YOUR_PASSWORD' \
  #   --from-literal=grafana_admin_password='YOUR_PASSWORD' \
  #   --from-literal=mlflow_db_password='YOUR_PASSWORD'
